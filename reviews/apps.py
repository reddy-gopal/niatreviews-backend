from django.apps import AppConfig


class ReviewsConfig(AppConfig):
    """Future: program/college reviews. Scaffold only for now."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "reviews"
    verbose_name = "Reviews"
    label = "reviews"
